---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";
import { getCollection } from "astro:content";
import createSlug from "../lib/createSlug"

const posts = (await getCollection("blog")).sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const last_posts = posts.slice(0, 3);
---

<BaseLayout sideBarActiveItemID="home">
  <div class="pb-12 mt-5">
    <div class="text-xl py-1">Hello world 👋</div>
    <div class="text-5xl font-bold">I'm Linkaili</div>
    <div class="text-3xl py-3 font-bold">Student Athlete & Golfer</div>
    <div class="py-2">
      <text class="text-lg">
        <b>Class of 2027</b> student based in the US, passionate about balancing the rigor of academics with the discipline of competitive golf.
        <br/><br/>
        On the course, I strive for precision and focus; in the classroom, I pursue knowledge in Science and Humanities.
        This website documents my journey, projects, and growth as I prepare for university.
      </text>
    </div>
    <div class="mt-8">
      <a class="btn" href="mailto:linkaili.2008@gmail.com" target="_blank"> Contact Me</a>
      <a class="btn btn-outline ml-5" href="/cv"> View My Resume</a>
    </div>
  </div>

  <div>
    <div class="text-3xl w-full font-bold mb-2">Featured Activities</div>
  </div>

  <HorizontalCard
    title="Varsity Golf Team Captain"
    img="/golf_match.jpg"
    desc="Leading the school team in regional tournaments. Responsible for team morale and practice coordination."
    url="/cv"
    badge="Leadership"
  />
  <div class="divider my-0"></div>
  <HorizontalCard
    title="Community Service Initiative"
    img="/volunteering.jpg"
    desc="Organizing local events to give back to the community and promote youth sports."
    url="/projects"
    badge="Volunteer"
  />

  <div>
    <div class="text-3xl w-full font-bold mb-5 mt-10">Recent Essays & Thoughts</div>
  </div>

  {
    last_posts.map((post) => (
      <>
        <HorizontalCard
          title={post.data.title}
          img="/study.jpg"
          desc={post.data.description}
          url={"/blog/" + createSlug(post.data.title, post.slug)}
          target="_self"
          badge={post.data.badge}
        />
        <div class="divider my-0" />
      </>
    ))
  }
</BaseLayout>
